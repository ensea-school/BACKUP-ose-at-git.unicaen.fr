<?php

/* @var $service Enseignement\Entity\Db\Service */
/* @var $params array */

$serviceListe = $this->enseignements([$service]);
/* @var $serviceListe Application\View\Helper\Service\Enseignements */

if (!empty($params)) $serviceListe->applyParams($params);

if ($onlyContent) {
    echo $this->serviceLigne($serviceListe, $service)->render($details);
} else {
    echo $serviceListe->renderLigne($service, $details);
}