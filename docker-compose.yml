version: '3'

# Ne pas oublier d'ajouter au fichier /etc/hosts :
# 172.21.0.10 ose-dev.pc-local
# 172.21.0.15 ose-test.pc-local

services:
  ose-dev:
    container_name: ose-dev

    environment:
      APPLICATION_ENV: dev
      APPLICATION_HOSTNAME: ose-dev.test
      XDEBUG_CONFIG: "remote_host=172.21.0.10"
      PHP_IDE_CONFIG: "serverName=ose-dev.test"
      IGNORE_MV_EXT_SERVICE: "true"

    build:
      context: .
      dockerfile: Dockerfile
      args:
        PHP_VERSION: 8.2
        APPLICATION_ENV: dev

    volumes:
      - .:/app

    working_dir: /app

    extra_hosts:
      - "host.docker.internal:host-gateway"

    networks:
      devnet:
        ipv4_address: 172.21.0.10



networks:
  devnet:
    external:
      name: laures
