CREATE OR REPLACE TRIGGER "INTERVENANT_DEL_DEFAUT"
  AFTER UPDATE OF HISTO_DESTRUCTION
  ON INTERVENANT
  REFERENCING FOR EACH ROW
BEGIN
  DELETE FROM intervenant_par_defaut WHERE intervenant_id = :OLD.id;
END;