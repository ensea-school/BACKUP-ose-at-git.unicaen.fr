<?php
if ($onlyContent){
    echo $this->serviceLigne( $entity )->setRenderIntervenants($renderIntervenants)->render($details);
}else{
    echo $this->serviceListe( array($entity) )->setRenderIntervenants($renderIntervenants)->renderLigne( $entity, $details );
}