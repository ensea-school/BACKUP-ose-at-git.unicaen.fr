<?php

if ($onlyContent){
    echo $this->serviceLigne( $service )
            ->setIntervenant($intervenant)
            ->setStructure($structure)
            ->setReadOnly($readOnly)
            ->setTypesIntervention($typesIntervention)
            ->render($details);
}else{
    echo $this->serviceListe( array($service) )
            ->setIntervenant($intervenant)
            ->setStructure($structure)
            ->setReadOnly($readOnly)
            ->setTypesIntervention($typesIntervention)
            ->renderLigne( $service, $details );
}